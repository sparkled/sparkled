/*global angular*/
(function () {
    'use strict';

    toastr.options = {
        closeButton: true,
        preventDuplicates: true,
        showDuration: 500,
        hideDuration: 500,
        timeOut: 3000,
        extendedTimeOut: 3000,
        showMethod: 'fadeIn',
        hideMethod: 'fadeOut',
        positionClass: 'toast-bottom-right'
    };
}());

