package io.sparkled.model.setting

interface SettingsConstants {

    interface BRIGHTNESS {
        companion object {
            val CODE = "BRIGHTNESS"
            val MIN = 0
            val MAX = 15
        }
    }
}
