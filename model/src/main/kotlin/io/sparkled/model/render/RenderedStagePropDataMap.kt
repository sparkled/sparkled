package io.sparkled.model.render

import java.util.HashMap
import java.util.UUID

/**
 * A map of stage prop UUID to rendered stage prop data.
 */
class RenderedStagePropDataMap : HashMap<UUID, RenderedStagePropData>()
